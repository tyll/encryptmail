logging:
     loglevel: debug

encryption:
    # comma-separated list of recipients
    recipients:

mta:
    smarthost:
        address:
        sha256_fpr:
        sender_address:
        local_hostname:
